<table>
  <thead>
    <tr>
      <th>Webinar date</th>
      <th>Title</th>
      <th>Presenter</th>
      <th>Attendees</th>
      <th colspan="1"></th>
    </tr>
  </thead>
  <tbody>
    <% webinars.each do |webinar|  %>
      <tr>
        <td><%= l(webinar.live_date, format: :short) %></td>
        <td><%= link_to webinar.title, admin_webinar_path(webinar) %></td>
        <td><%= webinar.presenter.name if webinar.presenter_id.present? %></td>
        <td><%= webinar.attendees.count %>
        <td><%= link_to "Duplicate", admin_webinar_duplicate_path(webinar) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
