<p>
  <strong>Date:</strong>
  <%= l(@webinar.live_date, format: :default) %>
</p>

  <h2><%= @webinar.title %></h2>
  <p><%= @webinar.description.html_safe %></p>

<p>
  <strong>Presenter:</strong>
  <%= @webinar.presenter.name if @webinar.presenter_id? %>
</p>
<%= @webinar.webinar_url %>

<section>
  <%= link_to 'Register', join_webinar_path(@webinar), class: 'btn' %>
  <%= link_to 'Back to list', webinars_path %>
</section>
