<p><%= @webinar.description %></p>

<p><span class="label">Live date:</span>
  <time><%= l(@webinar.live_date, format: :full_tz) %></time>
</p>

<p><span class="label">Presenter:</span>
  <%= @webinar.presenter.name if @webinar.presenter.present? %>
</p>

<%= link_to t("webinars.show.ics"), {controller: 'webinars', action: :show, format: :ics}, class: 'btn' %>
