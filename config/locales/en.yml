en:
  date:
    formats:
      default: "%d/%m/%Y"
      with_weekday: "%a %d/%m/%y"

  admin:
    webinars:
      index:
  
  attendees:
    index: 
      show: "Show"
      edit: "Edit"
      destroy: "Remove"
  helpers:
    actions:
      show: 'View #{model}'
      edit: "Change"
      back: "Back"
    links:
      cancel: "Cancel"
      destroy: "Remove"
      save: "Save"

  webinars:
    index:
      columns:
        date: "%a %d/%m/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: TrilbyTV Webinars
