en:
  date:
    formats:
      default: "%d/%m/%Y"
      with_weekday: "%a %d/%m/%y"

  admin:
    webinars:
      index:
  
  attendees:
    index: 
      show: "Show"
      edit: "Edit"
      destroy: "Remove"
  helpers:
    actions:
      show: 'View #{model.titleize}'
      edit: "Change"
      back: "Back"
    links:
      cancel: "Cancel"
      destroy: "Remove"
      save: "Save"

  webinars:
    index:
      columns:
        date: "%a %d/%m/%y"

  time:
    formats:
      default:
        "%A, %-d %b %Y at %r"
      date:
        "%-d %b %Y"
      short:
        "%d/%m/%Y %r"

  titles:
    application: Webinars | TrilbyTV
