en:
  date:
    formats:
      default:
        "%d/%m/%Y"
      with_weekday:
        "%a %d/%m/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Webinartv

  webinars:
    index:
      columns:
        date: Date
  admin:
    webinars:
      index:
        columns:
          date: Date
